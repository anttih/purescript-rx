{
  "private": true,
  "scripts": {
    "postinstall": "bower install",
    "build": "pulp build -I examples",
    "test": "pulp test -I examples --main Examples"
  },
  "dependencies": {
    "rxjs": "^5.0.0-beta.11"
  },
  "devDependencies": {
    "pulp": "^9.0.0",
    "purescript": "^0.9.1"
  }
}
