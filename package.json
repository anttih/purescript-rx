{
  "private": true,
  "scripts": {
    "postinstall": "psc-package update",
    "build": "pulp build -I examples",
    "test": "pulp test -I examples --main Examples"
  },
  "dependencies": {
    "rx": "4.1.0"
  },
  "devDependencies": {
    "pulp": "^12.0.0",
    "purescript": "^0.11.7"
  }
}
